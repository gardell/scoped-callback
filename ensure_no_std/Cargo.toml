[package]
name = "ensure_no_std"
version = "0.2.0"
authors = ["Johan Gardell <736172+gardell@users.noreply.github.com>"]
edition = "2018"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]
scoped-callback = { path = "..", default-features = false }
